# 三关 SwitchDoor 环境与数据生成协议 v1

## 1. 唯一目标

生成能够支持“真实反馈读规则 → 规则改变后续动作”的最小三关 SwitchDoor
episode，并保证数据可解、配对成立且隐藏规则不进入模型可见输入。

本协议只资格化环境和数据，不评价任何模型。

## 2. 固定机制

- 动作：上、下、左、右、`INTERACT`；
- 对象：玩家、红蓝开关、红蓝门、绿色目标与墙；
- 隐藏因素：`same_color` 或 `cross_color`；
- 门响应：固定为 `open_only`，打开后永久保持打开；
- 同一 episode 的三关共享隐藏映射；每关动态状态重置；
- 关卡数与尺寸固定为 3 关、`[7, 7, 11]`。

## 3. 三关合同

- L1：红开关探测在两种映射下分别打开不同颜色的门，两条门路都能通向目标；
- L2：红门是唯一目标入口，正确首个有效开关在两种映射下翻转；
- L3：沿用 L2 的规则使用要求，地图扩大到 11×11；两种映射的 oracle 最短
  通关动作数相同，避免把隐藏规则与路线难度混淆。

每关从注册模板独立采样一个 D4 旋转/镜像。变换只改变空间位置，不改变颜色、
规则或可解性。

## 4. 独立单位与配对

独立生成单位是 `root_id`：它固定三关布局变换。普通模式为每个 root 采样一个
隐藏映射；配对模式为同一 root 同时生成同色和异色 episode。配对 twins 的每关
初始 RGB 必须完全相同，只有交互后的真实反馈和相应监督动作可以不同。

## 5. 在线接口合同

在线环境必须直接复用本协议注册的布局、状态转移和 renderer，不得从已经包含完整
未来轨迹的 `public.json` 模拟交互，也不得复制第二套物理。

- evaluator 在私有侧用 `root_seed` 与 `switch_mapping` 建立 episode；二者不是模型输入；
- `reset` 建立 L1，只返回当前 `rgb_frame`、`available_actions`、`status` 和
  `previous_action`；
- `step(action)` 恰好执行一个五动作集合中的真实动作，再产生一个新观察；撞墙与空
  `INTERACT` 是会消耗动作的真实 no-op；
- L1/L2 到达 `WIN` 后必须先保留终止观察，再由 runner 零成本进入下一关；换关时
  agent、门和终止状态重置，三关共享的隐藏映射保持不变；
- L3 `WIN` 后 episode 终止，不允许继续产生幽灵动作；
- 关卡号、完成信号等 runner 管理字段必须与模型可见观察分区；事实账本、belief、
  planner、action cap 和评分都属于上层实验，不写入环境。

本地 Web 体验端可以显示关卡号、动作计数和用户选择的 seed，帮助人工操作；这些
runner 字段不属于正式模型输入。隐藏规则仅在三关全部完成后作为体验结果揭示。

## 6. 信息边界

模型可见数据只有当前 RGB、可用动作、状态、过去真实动作和过去真实观察。
在任意决策时刻，loader 只能读取截至当前观察的时间前缀；同一轨迹中尚未发生的
观察也属于未来信息，不得提前提供。

以下内容属于私有真值，不得拼入模型输入：隐藏映射、符号坐标、墙集合、门状态
真值、布局变换、oracle 未来动作和 oracle 距离。样本身份、生成 seed、数据集
索引和私有文件路径也不得成为模型输入。`public.json` 本身不写入 `episode_id` 或
`root_id`；`supervision.jsonl` 中的下一动作仅作为训练标签使用。

## 7. 必须通过的有效性检查

- 每个 episode 恰好三关且尺寸为 7×7、7×7、11×11；
- 三关共享同一个隐藏映射且门响应为 `open_only`；
- 每关在两种映射下均可解，保存轨迹能逐步精确回放至目标；
- L1 恰好执行一次交互，该交互固定发生在红开关，且两种映射打开不同门；
- L2/L3 首次有效开关在同色/异色条件下分别为红/蓝；
- 配对 twins 初始图片逐字节相同；
- 公开 JSON 不出现隐藏规则、符号地图或样本身份字段；
- 在线 observation 字段严格等于注册四字段，且不能取得未来观察、身份或私有真值；
- 在线逐步输出与同一状态转移及 renderer 的离线回放逐帧一致；
- L1→L2→L3 换关保持隐藏映射、重置关内动态状态，并保留每关真实 `WIN` 观察；
- 所有生成请求都留在固定分母中；生成或写盘失败必须显式报错。

这些检查失败意味着数据合同无效。模型表现差不属于生成器失败。

## 8. 声明边界

通过本协议只能说明：生成器能产出满足上述合同的合成三关数据。它不能说明模型
已经发现规则、使用历史、学会世界模型、完成 ARC-AGI-3 或具备未知机制泛化。
