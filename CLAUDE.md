# 执行说明

目标：运行并扩展三关 SwitchDoor 数据生成器，而不改变科学合同。

## 不可自行改变

- 三关数量和尺寸 `[7, 7, 11]`；
- 三关共享同一个 `same_color`/`cross_color` 隐藏映射；
- `open_only` 门响应；
- L1 的一次红开关探测，以及 L2/L3 的目标门控制关系；
- 配对 twins 的布局与初始像素一致性；
- 公开观察、监督标签和私有真值的隔离；
- 可解性、回放和首个有效开关检查。

## 可以适配

- Python 安装方式、输出路径和批处理脚本；
- 在不改变像素语义时提高渲染速度；
- 数据压缩、并行生成和服务器存储布局；
- 为训练框架添加只读取截至当前时刻的 `public.json` 前缀、frame 与对应监督标签的
  loader；模型输入只取注册字段，不传未来帧、样本 ID 或私有路径。

若需要改变关卡机制、隐藏因素、模板语义或模型可见字段，请新建协议版本，不要
静默沿用 `switchdoor-three-level-generator-v1`。
